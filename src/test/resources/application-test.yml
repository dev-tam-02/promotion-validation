spring:
  application:
    name: validation-test
  data:
    mongodb:
      uri: mongodb://localhost:27017/validation-test?retryWrites=false

validation:
  engine:
    base-url: http://localhost:8094
    timeout:
      connect: 5s
      read: 30s

logging:
  level:
    vn.viettel.vds.promotion.validation: DEBUG