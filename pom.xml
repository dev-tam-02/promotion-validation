<?xml version="1.0" encoding="UTF-8"?>
<project xmlns:xsi="http://www.w3.org/2001/XMLSchema-instance" xmlns="http://maven.apache.org/POM/4.0.0"
         xsi:schemaLocation="http://maven.apache.org/POM/4.0.0 https://maven.apache.org/xsd/maven-4.0.0.xsd">
    <modelVersion>4.0.0</modelVersion>
    <parent>
        <groupId>com.promix.platform</groupId>
        <artifactId>promix-starter-parent</artifactId>
        <version>2.0.0-SNAPSHOT</version>
        <relativePath/>
    </parent>
    <artifactId>validation</artifactId>
    <version>0.0.1-SNAPSHOT</version>
    <name>validation</name>
    <description>validation</description>
    <url/>
    <licenses>
        <license/>
    </licenses>
    <developers>
        <developer/>
    </developers>
    <scm>
        <connection/>
        <developerConnection/>
        <tag/>
        <url/>
    </scm>
    <dependencies>
        <!-- Schema module for shared DTOs -->
        <dependency>
            <groupId>vn.viettel.vds.promotion</groupId>
            <artifactId>schema</artifactId>
            <version>2.0.0-SNAPSHOT</version>
        </dependency>
        <dependency>
            <groupId>com.promix.platform</groupId>
            <artifactId>promix-web-spring-boot-starter</artifactId>
        </dependency>
        <dependency>
            <groupId>com.promix.platform</groupId>
            <artifactId>promix-error-spring-boot-starter</artifactId>
        </dependency>
        <dependency>
            <groupId>com.promix.platform</groupId>
            <artifactId>promix-validation-spring-boot-starter</artifactId>
        </dependency>
        <dependency>
            <groupId>com.promix.platform</groupId>
            <artifactId>promix-data-jpa-spring-boot-starter</artifactId>
        </dependency>
        <dependency>
            <groupId>com.promix.platform</groupId>
            <artifactId>promix-observer-spring-boot-starter</artifactId>
            <exclusions>
                <!-- Exclude Zipkin reporter to prevent Zipkin sender initialization -->
                <exclusion>
                    <groupId>io.zipkin.reporter2</groupId>
                    <artifactId>zipkin-reporter-brave</artifactId>
                </exclusion>
            </exclusions>
        </dependency>
        <dependency>
            <groupId>com.promix.platform</groupId>
            <artifactId>promix-resilience-spring-boot-starter</artifactId>
        </dependency>
        <dependency>
            <groupId>com.promix.platform</groupId>
            <artifactId>promix-cache-spring-boot-starter</artifactId>
            <exclusions>
                <exclusion>
                    <groupId>org.springframework.boot</groupId>
                    <artifactId>spring-boot-starter-data-redis</artifactId>
                </exclusion>
                <exclusion>
                    <groupId>io.lettuce</groupId>
                    <artifactId>lettuce-core</artifactId>
                </exclusion>
            </exclusions>
        </dependency>
        <dependency>
            <groupId>com.promix.platform</groupId>
            <artifactId>promix-messaging-spring-boot-starter</artifactId>
        </dependency>
        <!-- Spring Batch for outbox pattern -->
        <dependency>
            <groupId>org.springframework.boot</groupId>
            <artifactId>spring-boot-starter-batch</artifactId>
        </dependency>
        <dependency>
            <groupId>com.promix.platform</groupId>
            <artifactId>promix-http-spring-boot-starter</artifactId>
            <version>2.0.0-SNAPSHOT</version>
        </dependency>
        <dependency>
            <groupId>org.springframework.cloud</groupId>
            <artifactId>spring-cloud-starter-openfeign</artifactId>
        </dependency>
        <!-- JSON Schema Validation -->
        <dependency>
            <groupId>com.networknt</groupId>
            <artifactId>json-schema-validator</artifactId>
            <version>1.5.1</version>
        </dependency>

        <!-- MariaDB driver for JPA -->
        <dependency>
            <groupId>org.mariadb.jdbc</groupId>
            <artifactId>mariadb-java-client</artifactId>
            <scope>runtime</scope>
            <exclusions>
                <exclusion>
                    <groupId>org.slf4j</groupId>
                    <artifactId>jcl-over-slf4j</artifactId>
                </exclusion>
            </exclusions>
        </dependency>

        <!-- Liquibase for database migrations -->
        <dependency>
            <groupId>org.liquibase</groupId>
            <artifactId>liquibase-core</artifactId>
        </dependency>
        <dependency>
            <groupId>com.promix.platform</groupId>
            <artifactId>promix-outbox-jpa-spring-boot-starter</artifactId>
        </dependency>
        <dependency>
            <groupId>com.promix.platform</groupId>
            <artifactId>promix-auth-context-spring-boot-starter</artifactId>
        </dependency>

        <!-- Resilience4j Spring Boot 3 for @CircuitBreaker annotation support -->
        <dependency>
            <groupId>io.github.resilience4j</groupId>
            <artifactId>resilience4j-spring-boot3</artifactId>
            <version>2.3.0</version>
        </dependency>
        <!-- Force resilience4j-spring6 to 2.3.0 (BOM/transitive pulls 2.2.0 — missing RxJava3FallbackDecorator) -->
        <dependency>
            <groupId>io.github.resilience4j</groupId>
            <artifactId>resilience4j-spring6</artifactId>
            <version>2.3.0</version>
        </dependency>
        <dependency>
            <groupId>io.reactivex.rxjava3</groupId>
            <artifactId>rxjava</artifactId>
            <version>3.1.8</version>
        </dependency>

        <!-- Mustache template engine for DRL compilation -->
        <dependency>
            <groupId>com.github.jknack</groupId>
            <artifactId>handlebars</artifactId>
            <version>4.3.1</version>
        </dependency>

        <!-- WebFlux / WebClient for reactive HTTP calls to pp-rule-engine -->
        <dependency>
            <groupId>org.springframework.boot</groupId>
            <artifactId>spring-boot-starter-webflux</artifactId>
        </dependency>

        <!-- WireMock for integration tests -->
        <dependency>
            <groupId>org.wiremock</groupId>
            <artifactId>wiremock-standalone</artifactId>
            <version>3.9.1</version>
            <scope>test</scope>
        </dependency>
    </dependencies>

    <dependencyManagement>
        <dependencies>
            <dependency>
                <groupId>com.promix.platform</groupId>
                <artifactId>promix-bom</artifactId>
                <version>2.0.0-SNAPSHOT</version>
                <type>pom</type>
                <scope>import</scope>
            </dependency>
        </dependencies>
    </dependencyManagement>
</project>
